spring:
  application:
    name: zipkin-service
  cloud:
    config:
      profile: dev
      discovery:
        enabled: true
        service-id: config-server

#management:
#  metrics:
#    web:
#      server:
#        auto-time-requests: false